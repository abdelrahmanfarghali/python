#need paths Images and subdir for it with name Production
#can only find .jpg file extension for conversion
#name of input image must start with 'image (' and end with ').jpg'
#do not delete any of older produced images so if you have produced until image (8).png do not delete image (7).png and less